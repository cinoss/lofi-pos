{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the cashier app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    {
      "identifier": "shell:allow-open",
      "allow": [{ "url": "https?://.*" }]
    },
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/bouncer-mock",
          "sidecar": true,
          "args": true
        }
      ]
    }
  ]
}
